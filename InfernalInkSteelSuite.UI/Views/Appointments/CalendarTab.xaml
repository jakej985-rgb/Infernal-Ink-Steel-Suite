<UserControl x:Class="InfernalInkSteelSuite.UI.Views.Appointments.CalendarTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:InfernalInkSteelSuite.UI.ViewModels.Appointments"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:CalendarTabViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="DayButtonStyle">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="GlowBorder"
                                CornerRadius="6"
                                BorderThickness="1"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="GlowBorder" Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding HasAppointments}" Value="True">
                                <Setter TargetName="GlowBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="GlowBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="{DynamicResource AccentColor}"/>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Today" Command="{Binding TodayCommand}" Margin="5,5,20,5" Padding="10,2"/>
            <Button Content="&lt;" Command="{Binding PreviousMonthCommand}" Margin="5"/>
            <TextBlock Text="{Binding CurrentMonthYear}" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Margin="10,0"/>
            <Button Content="&gt;" Command="{Binding NextMonthCommand}" Margin="5"/>
        </StackPanel>

        <!-- Days of the Week -->
        <UniformGrid Grid.Row="1" Rows="1" Columns="7" Margin="5,0">
            <TextBlock Text="Sunday" HorizontalAlignment="Center"/>
            <TextBlock Text="Monday" HorizontalAlignment="Center"/>
            <TextBlock Text="Tuesday" HorizontalAlignment="Center"/>
            <TextBlock Text="Wednesday" HorizontalAlignment="Center"/>
            <TextBlock Text="Thursday" HorizontalAlignment="Center"/>
            <TextBlock Text="Friday" HorizontalAlignment="Center"/>
            <TextBlock Text="Saturday" HorizontalAlignment="Center"/>
        </UniformGrid>

        <!-- Calendar Grid -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding Days}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="7"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.DayClickedCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Style="{StaticResource DayButtonStyle}"
                            Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                        <Button.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Add Appointment" 
                                          Command="{Binding PlacementTarget.Tag.AddAppointmentCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" 
                                          CommandParameter="{Binding PlacementTarget.DataContext.Date, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                        <StackPanel>
                            <TextBlock Text="{Binding DayNumber}" FontWeight="Bold"/>
                            <ItemsControl ItemsSource="{Binding AppointmentSummaries}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding Color}" CornerRadius="3" Margin="0,1" Padding="4,2" ToolTip="{Binding TooltipText}">
                                            <TextBlock Text="{Binding DisplayText}" FontSize="10" Foreground="White" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
