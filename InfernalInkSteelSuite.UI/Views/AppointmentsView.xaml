<UserControl x:Class="InfernalInkSteelSuite.UI.Views.AppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl x:Name="MainTabWidget" Grid.Row="0" SelectionChanged="MainTabWidget_SelectionChanged">
            <TabItem Header="Upcoming" x:Name="UpcomingTab"/>
            <TabItem Header="Pending" x:Name="PendingTab"/>
            <TabItem Header="Completed" x:Name="CompletedTab"/>
            <TabItem Header="Calendar" x:Name="CalendarTab"/>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10">
            <Button x:Name="AddButton" Content="Add" Margin="5"/>
            <Button x:Name="EditButton" Content="Edit" Margin="5"/>
            <Button x:Name="DeleteButton" Content="Delete" Margin="5"/>
            <Button x:Name="RefreshButton" Content="Refresh" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
