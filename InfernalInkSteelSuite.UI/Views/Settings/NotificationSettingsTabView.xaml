<UserControl x:Class="InfernalInkSteelSuite.UI.Views.Settings.NotificationSettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Notification Settings" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextColor}" Margin="0,0,0,20"/>

            <Expander IsExpanded="True" Margin="0,0,0,10">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📧" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Appointment Reminders" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                    </StackPanel>
                </Expander.Header>
                <StackPanel Margin="10">
                    <CheckBox Content="Email Reminders" IsChecked="{Binding EmailAppointmentReminders}" Foreground="{DynamicResource TextColor}" Margin="0,5"/>
                    <CheckBox Content="SMS Reminders" IsChecked="{Binding SmsAppointmentReminders}" Foreground="{DynamicResource TextColor}" Margin="0,5"/>
                    <CheckBox Content="Desktop Notifications" IsChecked="{Binding DesktopNotifications}" Foreground="{DynamicResource TextColor}" Margin="0,5"/>
                    
                    <Label Content="Reminder Timing" Foreground="{DynamicResource TextColor}" FontWeight="Medium" Margin="0,10,0,0"/>
                    <ComboBox ItemsSource="{Binding ReminderTimingOptions}" SelectedItem="{Binding ReminderTiming}" Margin="0,5"/>
                </StackPanel>
            </Expander>

            <Button Content="Save Settings" Command="{Binding SaveNotificationSettingsCommand}" HorizontalAlignment="Right" Style="{StaticResource PrimaryButtonStyle}" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
