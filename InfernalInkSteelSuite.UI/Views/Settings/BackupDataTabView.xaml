<UserControl x:Class="InfernalInkSteelSuite.UI.Views.Settings.BackupDataTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Backup &amp; Data Management" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextColor}" Margin="0,0,0,20"/>

            <Expander IsExpanded="True" Margin="0,0,0,10">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Backup Settings" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                    </StackPanel>
                </Expander.Header>
                <StackPanel Margin="10">
                    <Label Content="Backup Location" Foreground="{DynamicResource TextColor}" FontWeight="Medium"/>
                    <Grid Margin="0,5,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding BackupPath}" IsReadOnly="True" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseBackupPathCommand}"/>
                    </Grid>

                    <CheckBox Content="Enable Automatic Backups" IsChecked="{Binding AutoBackupEnabled}" Foreground="{DynamicResource TextColor}" Margin="0,5"/>
                    
                    <Label Content="Backup Frequency" Foreground="{DynamicResource TextColor}" FontWeight="Medium" Margin="0,10,0,0"/>
                    <ComboBox ItemsSource="{Binding BackupFrequencyOptions}" SelectedItem="{Binding BackupFrequency}" Margin="0,5"/>

                    <Label Content="Retention Period (days)" Foreground="{DynamicResource TextColor}" FontWeight="Medium" Margin="0,10,0,0"/>
                    <TextBox Text="{Binding RetentionDays}" Margin="0,5"/>
                </StackPanel>
            </Expander>

            <Expander IsExpanded="True" Margin="0,0,0,10">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📤" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Manual Backup &amp; Export" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                    </StackPanel>
                </Expander.Header>
                <StackPanel Margin="10">
                    <Button Content="Create Backup Now" Command="{Binding CreateBackupCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,5"/>
                    <Button Content="Export Data (JSON/CSV)" Command="{Binding ExportDataCommand}" Margin="0,5"/>
                </StackPanel>
            </Expander>

            <Button Content="Save Settings" Command="{Binding SaveBackupSettingsCommand}" HorizontalAlignment="Right" Style="{StaticResource PrimaryButtonStyle}" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
