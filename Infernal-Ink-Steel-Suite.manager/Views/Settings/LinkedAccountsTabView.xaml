<UserControl x:Class="InfernalInkSteelSuite.Views.Settings.LinkedAccountsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:InfernalInkSteelSuite.Views.Settings"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Integrations &amp; Social Media" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextColor}" Margin="0,0,0,20"/>

            <Expander IsExpanded="True" Margin="0,0,0,10">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌐" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Social Media Links" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                    </StackPanel>
                </Expander.Header>
                <StackPanel Margin="10">
                    <Label Content="Instagram URL" Foreground="{DynamicResource TextColor}" FontWeight="Medium"/>
                    <TextBox Text="{Binding InstagramUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <Label Content="Facebook URL" Foreground="{DynamicResource TextColor}" FontWeight="Medium"/>
                    <TextBox Text="{Binding FacebookUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <Label Content="Twitter/X URL" Foreground="{DynamicResource TextColor}" FontWeight="Medium"/>
                    <TextBox Text="{Binding TwitterUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

                    <Label Content="Website URL" Foreground="{DynamicResource TextColor}" FontWeight="Medium"/>
                    <TextBox Text="{Binding WebsiteUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                </StackPanel>
            </Expander>

            <Expander IsExpanded="False" Margin="0,0,0,10">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💳" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Payment Processors" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                    </StackPanel>
                </Expander.Header>
                <StackPanel Margin="10">
                    <TextBlock Text="Payment processor integration coming soon" Foreground="{DynamicResource TextColor}" FontStyle="Italic"/>
                </StackPanel>
            </Expander>

            <Expander IsExpanded="False" Margin="0,0,0,10">
                <Expander.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📅" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Calendar Sync" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                    </StackPanel>
                </Expander.Header>
                <StackPanel Margin="10">
                    <TextBlock Text="Calendar synchronization coming soon" Foreground="{DynamicResource TextColor}" FontStyle="Italic"/>
                </StackPanel>
            </Expander>

            <Button Content="Save Settings" Command="{Binding SaveIntegrationsCommand}" HorizontalAlignment="Right" Style="{StaticResource PrimaryButtonStyle}" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
