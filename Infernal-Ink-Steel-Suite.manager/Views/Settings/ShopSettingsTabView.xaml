<UserControl x:Class="InfernalInkSteelSuite.Views.Settings.ShopSettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:InfernalInkSteelSuite.Views.Settings"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <Label Content="Shop Name" Foreground="{DynamicResource TextColor}"/>
            <TextBox Text="{Binding ShopName, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}"/>
            <Label Content="Tattoo Rate (per hour)" Foreground="{DynamicResource TextColor}"/>
            <TextBox Text="{Binding TattooRate, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}"/>
            <Label Content="Piercing Rate (single)" Foreground="{DynamicResource TextColor}"/>
            <TextBox Text="{Binding PiercingSingle, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}"/>
            <Label Content="Piercing Rate (multiple)" Foreground="{DynamicResource TextColor}"/>
            <TextBox Text="{Binding PiercingMulti, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource TextBoxStyle}"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
