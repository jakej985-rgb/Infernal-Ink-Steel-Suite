<UserControl x:Class="InfernalInkSteelSuite.Views.Settings.ShopHoursTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Shop Schedule" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextColor}" Margin="0,0,0,20"/>

            <!-- Weekly Hours -->
            <Expander IsExpanded="True" Margin="0,0,0,10">
                <Expander.Header>
                    <TextBlock Text="📅 Standard Operational Hours" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                </Expander.Header>
                <ItemsControl ItemsSource="{Binding ShopHours}" Margin="15">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding DayName}" VerticalAlignment="Center" Foreground="{DynamicResource TextColor}" FontWeight="Medium"/>
                                <CheckBox IsChecked="{Binding IsOpen}" Content="Open" Margin="10,0" VerticalAlignment="Center" Foreground="{DynamicResource TextColor}"/>
                                <StackPanel Orientation="Horizontal" Grid.Column="2" Visibility="{Binding IsOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ComboBox ItemsSource="{Binding TimeSlots}" SelectedItem="{Binding SelectedStartTime}" Width="110" Margin="5,0"/>
                                    <TextBlock Text="to" VerticalAlignment="Center" Foreground="{DynamicResource TextColor}" Margin="5,0"/>
                                    <ComboBox ItemsSource="{Binding TimeSlots}" SelectedItem="{Binding SelectedEndTime}" Width="110" Margin="5,0"/>
                                </StackPanel>
                                <Button Grid.Column="3" Content="Copy to All" Command="{Binding DataContext.CopyToAllDaysCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" 
                                        Margin="15,0,0,0" Visibility="{Binding IsOpen, Converter={StaticResource BooleanToVisibilityConverter}}" ToolTip="Copy these hours to all other days"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <!-- Special Hours / Holidays -->
            <Expander IsExpanded="True" Margin="0,0,0,10">
                <Expander.Header>
                    <TextBlock Text="🎉 Special Dates &amp; Holidays" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextColor}"/>
                </Expander.Header>
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Calendar SelectedDate="{Binding SelectedSpecialDate}" Margin="0,0,25,0" VerticalAlignment="Top"/>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding SelectedSpecialDate, StringFormat='{}{0:D}'}" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextColor}"/>
                        <CheckBox IsChecked="{Binding IsSpecialDayClosed}" Content="Closed all day" Margin="0,0,0,15" Foreground="{DynamicResource TextColor}"/>
                        <Label Content="Event Description (e.g., 'Shop Renovation')" Foreground="{DynamicResource TextColor}"/>
                        <TextBox Text="{Binding SpecialDayDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
                        <Button Content="Add / Update Special Day" Command="{Binding AddSpecialDayCommand}" HorizontalAlignment="Left" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,25"/>
                        
                        <TextBlock Text="Manage Planned Events" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{DynamicResource TextColor}"/>
                        <DataGrid ItemsSource="{Binding SpecialHours}" AutoGenerateColumns="False" CanUserAddRows="False" MaxHeight="180" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:d}'}" IsReadOnly="True" Width="100"/>
                                <DataGridCheckBoxColumn Header="Closed" Binding="{Binding IsClosed}" IsReadOnly="True" Width="60"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding Description}" IsReadOnly="True" Width="*"/>
                                <DataGridTemplateColumn Width="50">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="❌" Command="{Binding DataContext.RemoveSpecialDayCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Foreground="Red" Background="Transparent" BorderThickness="0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Grid>
            </Expander>

            <Button Content="Save Schedule Settings" Command="{Binding SaveSettingsCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Margin="0,25,0,0" Padding="25,10"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
