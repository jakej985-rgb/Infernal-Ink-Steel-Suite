<UserControl x:Class="InfernalInkSteelSuite.Views.Settings.UserProfileTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:InfernalInkSteelSuite.Views.Settings"
             xmlns:attachedProperties="clr-namespace:InfernalInkSteelSuite.AttachedProperties"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <Label Content="New Password" Foreground="{DynamicResource TextColor}"/>
            <PasswordBox attachedProperties:PasswordBoxHelper.BoundPassword="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource PasswordBoxStyle}"/>
            <Label Content="Confirm New Password" Foreground="{DynamicResource TextColor}"/>
            <PasswordBox attachedProperties:PasswordBoxHelper.BoundPassword="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource PasswordBoxStyle}"/>
            <Button Content="Change Password" Command="{Binding ChangePasswordCommand}" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
