<UserControl x:Class="InfernalInkSteelSuite.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:InfernalInkSteelSuite.ViewModels.Settings"
             xmlns:views="clr-namespace:InfernalInkSteelSuite.Views.Settings"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodels:UserManagementTabViewModel}">
            <views:UserManagementTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ShopHoursTabViewModel}">
            <views:ShopHoursTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:PricingTabViewModel}">
            <views:PricingTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ShopProfileTabViewModel}">
            <views:ShopProfileTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:UserTabViewModel}">
            <views:UserTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:LinkedAccountsTabViewModel}">
            <views:LinkedAccountsTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:NotificationSettingsTabViewModel}">
            <views:NotificationSettingsTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:BackupDataTabViewModel}">
            <views:BackupDataTabView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AccessibilityTabViewModel}">
            <views:AccessibilityTabView/>
        </DataTemplate>
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" BorderThickness="0" Margin="0,0,-1,0">
                            <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF00FFFF" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,3,0" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter TargetName="Border" Property="BorderBrush" Value="Transparent" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <TabControl ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}" TabStripPlacement="Left" Background="{DynamicResource AppBackgroundBrush}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Header}" Foreground="{DynamicResource TextColor}" FontSize="16"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
    </Grid>
</UserControl>
