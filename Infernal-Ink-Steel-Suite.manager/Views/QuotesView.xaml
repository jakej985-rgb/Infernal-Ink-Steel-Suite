<UserControl x:Class="InfernalInkSteelSuite.Views.QuotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Infernal_Ink_Steel_Suite.manager.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:QuotesViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding AverageTattooPrice, StringFormat='Average Tattoo Price/hr: ${0:F2}'}" />
        <Label Grid.Row="1" Content="{Binding AveragePiercingPrice, StringFormat='Average Piercing Price/hr: ${0:F2}'}" />

        <DataGrid Grid.Row="2" ItemsSource="{Binding Quotes}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Service" Binding="{Binding Service}" Width="*"/>
                <DataGridTextColumn Header="Price Charged" Binding="{Binding PriceCharged, StringFormat=${0:F2}}" Width="Auto"/>
                <DataGridTextColumn Header="Duration (min)" Binding="{Binding DurationMinutes}" Width="Auto"/>
                <DataGridTextColumn Header="Price/hr" Binding="{Binding PricePerHour, StringFormat=${0:F2}}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="Refresh Quotes" Command="{Binding LoadQuotesCommand}" HorizontalAlignment="Left" Margin="5"/>
    </Grid>
</UserControl>
