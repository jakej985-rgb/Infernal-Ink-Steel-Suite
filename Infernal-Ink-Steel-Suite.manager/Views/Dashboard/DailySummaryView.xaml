<Window x:Class="InfernalInkSteelSuite.Views.Dashboard.DailySummaryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:InfernalInkSteelSuite.ViewModels.Dashboard"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:DailySummaryViewModel}"
        Title="Daily Summary" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Daily Summary" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Center"/>
        <TextBlock Text="{Binding CurrentDate}" FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}" HorizontalAlignment="Center" Grid.Row="0" Margin="0,30,0,0"/>

        <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" CornerRadius="10" Margin="0,20" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="10">
                    <TextBlock Text="Completed Appointments" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <TextBlock Text="{Binding CompletedAppointments}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Margin="10">
                    <TextBlock Text="Total Revenue" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=C}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="10">
                    <TextBlock Text="No-Shows / Cancellations" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <TextBlock Text="{Binding NoShows}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10">
                    <TextBlock Text="Outstanding Balances" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    <TextBlock Text="{Binding OutstandingBalances, StringFormat=C}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Print / Export" Command="{Binding PrintCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Close" Command="{Binding CloseCommand}" Padding="10,5"/>
        </StackPanel>
    </Grid>
</Window>
