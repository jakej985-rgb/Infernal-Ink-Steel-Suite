<Page x:Class="Infernal_Ink_Steel_Suite.manager.AppointmentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AppointmentsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="Add" Margin="5" Click="AddButton_Click"/>
            <Button Content="Edit" Margin="5" Click="EditButton_Click"/>
            <Button Content="Delete" Margin="5" Click="DeleteButton_Click"/>
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="Upcoming">
                <DataGrid x:Name="UpcomingAppointmentsGrid"/>
            </TabItem>
            <TabItem Header="Pending">
                <DataGrid x:Name="PendingAppointmentsGrid"/>
            </TabItem>
            <TabItem Header="Completed">
                <DataGrid x:Name="CompletedAppointmentsGrid"/>
            </TabItem>
            <TabItem Header="Calendar">
                <Calendar x:Name="AppointmentsCalendar"/>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
